services:
  server:
    build:
      context: .
      target: final
      args:
        - BASE_URL=$BASE_URL
        - BIND_PORT=$BIND_PORT
    ports:
      - "127.0.0.1:$BIND_PORT:$BIND_PORT/tcp"
    environment:
      - DATABASE_PASSWORD
      - DATABASE_URL=postgresql://postgres:${DATABASE_PASSWORD}@db/fanschnick
      - BASE_URL
      - BIND_PORT
    depends_on:
      db:
        condition: service_healthy
  db:
    image: postgres:18.1
    restart: always
    env_file: .env
    secrets:
      - db-password
    volumes:
      - db-data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=fanschnick
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
    expose:
      - 5432
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
volumes:
  db-data:
secrets:
  db-password:
    environment: DATABASE_PASSWORD
